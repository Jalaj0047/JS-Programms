const subSet=(str)=>{
   let arr=[];
   let i=0;
   let j=0;
   let str1="";
   while(i<arr.length){
       str1=str1+str[i];
       arr.push(str1);
       console.log(arr);
   }
}
subSet("dog");